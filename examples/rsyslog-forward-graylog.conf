# Snippet for /etc/rsyslog.d/ on Linux: forward local auth + syslog to Graylog Syslog UDP (RFC3164).
# Replace GRAYLOG_HOST with your Docker host IP or 127.0.0.1 if rsyslog runs on the same machine.
#
# module(load="imuxsock")
# template(name="GraylogFormat" type="string" string="<%PRI%>%TIMESTAMP:::date-rfc3164% %HOSTNAME% %syslogtag%%msg:::sp-if-no-1st-sp%%msg:::drop-last-lf%\n")
# *.* @GRAYLOG_HOST:5140;GraylogFormat
